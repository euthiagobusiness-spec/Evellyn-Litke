# Opcional: o projeto já usa a URL pública padrão do Supabase.
VITE_SUPABASE_URL=https://SEU_PROJECT_REF.supabase.co

# Secrets das Edge Functions devem ser configurados no painel do Supabase,
# nunca em variáveis VITE_* ou no repositório.
