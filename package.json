{
  "name": "funil-evellyn-litke",
  "version": "1.0.0",
  "private": true,
  "description": "Funil único com captura direta, upsell e documentos legais.",
  "scripts": {
    "dev": "node local-server.js",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "vite": "8.1.4"
  }
}
